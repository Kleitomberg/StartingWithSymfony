{% extends 'base.html.twig' %}

{% block title %}Editar Livro{% endblock %}

{% block body %}

<div class="container">
    <h1 class="text-center mt-3">Editar Livro</h1>

    {{ include('livro/_form.html.twig', {'button_label': 'Atualizar'}) }}

<div class="d-flex gap-3">
    <a class="btn btn-warning" href="{{ path('app_livro_index') }}">back to list</a>

    {{ include('livro/_delete_form.html.twig') }}
</div>
    </div>
{% endblock %}
